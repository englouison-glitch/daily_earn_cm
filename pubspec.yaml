name: dailyearn_cm
description: DailyEarn CM — Revenu complémentaire, créé par Louison Eng
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.0
  firebase_auth: ^4.15.0
  cloud_firestore: ^4.13.0
  google_mobile_ads: ^4.0.0
  share_plus: ^7.2.0
  provider: ^6.1.1

flutter:
  uses-material-design: true
  assets:
    - assets/locale/fr.json
    - assets/locale/en.json

